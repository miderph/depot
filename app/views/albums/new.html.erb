<h1>New album</h1>

<% form_for(@album,:html=>{:multipart=>true}) do |f| %>
  <%= f.error_messages %>
  <p>
    <%= f.label 'photo','photo'%>
    <%= f.file_field 'photo'%>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', albums_path %>